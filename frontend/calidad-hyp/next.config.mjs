/** @type {import('next').NextConfig} */

const nextConfig = {
  reactCompiler: true,

  basePath: "/calidad-hyp",
  assetPrefix: "/calidad-hyp",
  trailingSlash: true,
};

export default nextConfig;
